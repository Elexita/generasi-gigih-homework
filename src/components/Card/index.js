import SongCard from "../Songcard";

const Card = ({ data }) => {
    return (
        <div>
                <SongCard key={music.id} data={music} />
            ))}
        </div>
    );
};

export default Card;